#Tuples
#Tuples are very similar to list but there is one difference that tuples are immutable which means they cannot be changed.
#Once an element is inside the tuples it cannot be reassigned.
#Tuples uses parenthesis(1,2,3)
#Two basic built in method of tuples are : INDEX METHOD and COUNT METHOD
t = ('A', 'B','C','A','D','A')
t
t.count('A')
t.count('B')
t.index('A')
t.index('C')